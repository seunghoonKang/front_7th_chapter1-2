# 기능 설계 → 테스트 설계 인수인계

## 작업 요약

반복 일정 기능에 대한 상세 기능 명세서를 작성했습니다.

1. **기존 코드베이스 분석 완료**:
   - ✅ `recurrenceUtils.ts`: 반복 인스턴스 생성 로직이 이미 완벽하게 구현되어 있음 (31일, 윤년 처리 포함)
   - ✅ `server.js`: 필요한 모든 API 엔드포인트가 이미 구현되어 있음 (`/api/events-list`, `/api/recurring-events/:repeatId`)
   - ✅ 타입 정의: RepeatType, RepeatInfo가 이미 존재 (RepeatInfo에 id 필드만 추가 필요)
   - ⚠️ UI: 반복 설정 폼이 주석 처리되어 있어 활성화 필요

2. **핵심 요구사항 정의**:
   - 반복 유형 선택 (매일/매주/매월/매년)
   - 특수 날짜 처리 (31일 매월, 윤년 29일 매년)
   - 반복 일정 표시 (아이콘으로 구분)
   - 반복 일정 수정 (단일/전체 선택 다이얼로그)
   - 반복 일정 삭제 (단일/전체 선택 다이얼로그)

3. **제외 범위 명확화**:
   - 반복 일정 겹침 검사 안함 (요구사항에서 명시)
   - 반복 유형/간격/종료일 수정 불가 (새로 생성 필요)
   - 특정 날짜부터 이후만 수정/삭제 불가

## 주요 결정사항

### 1. 기존 유틸리티 100% 재사용
**이유**: `recurrenceUtils.ts`의 `generateInstancesForEvent`와 `getNextOccurrence` 함수가 이미 완벽하게 구현되어 있음. 31일 매월 반복과 윤년 2월 29일 처리 로직이 정확함.

**결과**: 새로운 날짜 계산 로직 작성 불필요, 기존 테스트 케이스도 활용 가능.

### 2. API 엔드포인트 기존 구조 활용
**이유**: server.js에 필요한 모든 API가 이미 구현되어 있음:
- `POST /api/events-list`: 반복 인스턴스 일괄 생성
- `PUT /api/recurring-events/:repeatId`: 반복 시리즈 수정
- `DELETE /api/recurring-events/:repeatId`: 반복 시리즈 삭제

**결과**: 서버 수정 불필요, 프론트엔드 훅에서 API 호출 함수만 추가하면 됨.

### 3. 수정/삭제 다이얼로그 패턴
**선택한 방식**: "해당 일정만 수정/삭제하시겠어요?" + "예/아니오" 버튼

**고려한 대안**:
- "이 일정만" / "이 일정 및 향후 일정" / "모든 일정" (3가지 선택)
  - ❌ 복잡도 증가, 요구사항에 없음
- 라디오 버튼으로 선택 후 확인
  - ❌ 클릭 횟수 증가, UX 저하

**선택 이유**: 요구사항에 명시된 정확한 문구 사용, 간단하고 명확한 선택지.

### 4. 반복 일정 겹침 검사 제외
**이유**: 요구사항에서 명시적으로 "반복일정은 일정 겹침을 고려하지 않는다"고 명시.

**결과**: `addOrUpdateEvent`에서 반복 일정 생성 시 `findOverlappingEvents` 호출 건너뜀.

### 5. RepeatInfo 타입에 id 필드 추가
**이유**: server.js에서 `repeat.id`를 사용하지만 타입 정의에 없음. 타입 안전성 확보 필요.

**변경**:
```typescript
export interface RepeatInfo {
  type: RepeatType;
  interval: number;
  endDate?: string;
  id?: string; // 추가
}
```

### 6. 반복 종료일 최대값 제한
**이유**: 요구사항에서 "2025-12-31까지 최대 일자를 만들어 주세요"라고 명시.

**구현**: 
- TextField에 `max="2025-12-31"` 속성 추가
- `generateInstancesForEvent`의 rangeEnd를 2025-12-31로 제한

## 테스트 설계 에이전트를 위한 노트

### ⚠️ 특별히 주의해야 할 엣지 케이스

1. **31일 매월 반복 (Critical)**:
   - 테스트 시나리오: 1월 31일에 매월 반복 생성 → 2월은 건너뛰고 3월 31일 생성 확인
   - 검증 포인트: 31일이 없는 달(2, 4, 6, 9, 11월)은 인스턴스가 생성되지 않아야 함
   - 기존 구현: `getNextOccurrence`의 98-123라인에서 처리

2. **윤년 2월 29일 매년 반복 (Critical)**:
   - 테스트 시나리오: 2024-02-29에 매년 반복 생성 → 2025, 2026, 2027은 건너뛰고 2028-02-29 생성 확인
   - 검증 포인트: 윤년이 아닌 해는 인스턴스가 생성되지 않아야 함
   - 기존 구현: `getNextOccurrence`의 125-151라인에서 처리

3. **반복 일정 수정 후 repeat.type 변경 (Important)**:
   - 테스트 시나리오: 반복 일정 → "해당 일정만 수정" → repeat.type이 'none'으로 변경되는지 확인
   - 검증 포인트: 단일 수정 후 해당 이벤트의 repeat.type === 'none', repeat.id === undefined

4. **존재하지 않는 repeatId로 수정/삭제 (Edge Case)**:
   - 테스트 시나리오: 잘못된 repeatId로 API 호출
   - 검증 포인트: 404 에러 처리, 적절한 에러 메시지 표시

5. **1000개 인스턴스 제한 (Safety)**:
   - 테스트 시나리오: 매일 반복 + 먼 미래 종료일
   - 검증 포인트: 최대 1000개까지만 생성 (무한 루프 방지)
   - 기존 구현: `generateInstancesForEvent`의 28라인 조건

6. **반복 일정 겹침 검사 제외 (Requirement)**:
   - 테스트 시나리오: 겹치는 반복 일정 생성
   - 검증 포인트: 겹침 경고 다이얼로그가 표시되지 않아야 함

### 💡 테스트 우선순위 제안

#### Priority 1 (Must Have - 핵심 기능)
1. ✅ 반복 인스턴스 생성 (`generateInstancesForEvent`)
   - 매일/매주/매월/매년 각각 테스트
   - 31일 매월, 윤년 29일 특수 케이스
   
2. ✅ 반복 시리즈 수정 API 호출 (`updateRecurringSeries`)
   - 전체 수정 시 모든 인스턴스 업데이트 확인
   
3. ✅ 반복 시리즈 삭제 API 호출 (`deleteRecurringSeries`)
   - 전체 삭제 시 모든 인스턴스 삭제 확인
   
4. ✅ 단일 수정/삭제
   - repeat.type 변환 확인
   - 다른 인스턴스 유지 확인

#### Priority 2 (Should Have - UI/UX)
5. ✅ 수정/삭제 다이얼로그 표시
   - repeat.type !== 'none'일 때만 표시
   - "예"/"아니오" 버튼 동작
   
6. ✅ 반복 아이콘 표시
   - 캘린더 뷰에 아이콘 렌더링
   - 단일 일정과 시각적 구분

#### Priority 3 (Nice to Have - 엣지 케이스)
7. ✅ API 에러 처리
   - 네트워크 오류 시 스낵바 표시
   - 404 에러 처리
   
8. ✅ 경계 조건
   - 반복 종료일 최대값 (2025-12-31)
   - 1000개 인스턴스 제한
   - 과거 날짜 반복 생성

### 🔗 참고할 기존 테스트 패턴

#### 1. 유닛 테스트 패턴
- **파일**: `src/__tests__/unit/easy.recurrenceUtils.spec.ts` (존재 가능성 확인 필요)
- **패턴**: 순수 함수 테스트, 입력-출력 검증
- **활용**: `generateInstancesForEvent`, `getNextOccurrence` 테스트에 적용

#### 2. Hook 테스트 패턴
- **파일**: `src/__tests__/hooks/medium.useEventOperations.spec.ts`
- **패턴**: `renderHook`, `waitFor`, `act` 사용
- **활용**: 새로운 hook 함수들 테스트에 적용
- **예시**:
```typescript
it('should create recurring events', async () => {
  const { result } = renderHook(() => useEventOperations(false));
  await act(async () => {
    await result.current.saveRecurringEvents(mockEventForm);
  });
  expect(result.current.events.length).toBeGreaterThan(1);
});
```

#### 3. 통합 테스트 패턴
- **파일**: `src/__tests__/medium.integration.spec.tsx`
- **패턴**: 컴포넌트 렌더링 → 사용자 인터랙션 → 결과 검증
- **활용**: 전체 반복 일정 생성-수정-삭제 흐름 테스트
- **시나리오**:
  1. 반복 체크박스 선택
  2. 반복 유형/간격/종료일 입력
  3. "일정 추가" 클릭
  4. 캘린더에 여러 인스턴스 표시 확인
  5. 하나 선택 후 수정 → 다이얼로그 → "아니오" → 전체 수정 확인
  6. 하나 선택 후 삭제 → 다이얼로그 → "예" → 단일 삭제 확인

#### 4. Mock 패턴
- **파일**: `src/__mocks__/handlers.ts`, `src/__mocks__/handlersUtils.ts`
- **패턴**: MSW로 API 모킹
- **활용**: 반복 관련 API 엔드포인트 모킹 추가 필요
- **추가할 핸들러**:
  - `POST /api/events-list`
  - `PUT /api/recurring-events/:repeatId`
  - `DELETE /api/recurring-events/:repeatId`

### 📋 테스트 체크리스트

#### 유닛 테스트
- [ ] `generateInstancesForEvent` - 매일 반복
- [ ] `generateInstancesForEvent` - 매주 반복
- [ ] `generateInstancesForEvent` - 매월 반복
- [ ] `generateInstancesForEvent` - 매년 반복
- [ ] `generateInstancesForEvent` - 31일 매월 (2월 건너뛰기)
- [ ] `generateInstancesForEvent` - 윤년 29일 매년 (평년 건너뛰기)
- [ ] `generateInstancesForEvent` - 1000개 제한
- [ ] `generateInstancesForEvent` - 종료일 이후 생성 안함

#### Hook 테스트
- [ ] `saveRecurringEvents` - 성공
- [ ] `saveRecurringEvents` - API 오류
- [ ] `updateRecurringSeries` - 전체 수정
- [ ] `updateRecurringSeries` - 404 오류
- [ ] `deleteRecurringSeries` - 전체 삭제
- [ ] `deleteRecurringSeries` - 404 오류
- [ ] 단일 수정 - repeat.type 변환
- [ ] 단일 삭제 - 다른 인스턴스 유지

#### 통합 테스트
- [ ] 반복 일정 생성 → 캘린더에 여러 인스턴스 표시
- [ ] 반복 아이콘 표시
- [ ] 수정 다이얼로그 - "예" 선택 → 단일 수정
- [ ] 수정 다이얼로그 - "아니오" 선택 → 전체 수정
- [ ] 삭제 다이얼로그 - "예" 선택 → 단일 삭제
- [ ] 삭제 다이얼로그 - "아니오" 선택 → 전체 삭제
- [ ] 단일 일정 수정/삭제 - 다이얼로그 표시 안함
- [ ] 반복 종료일 최대값 (2025-12-31) 제한

## 참조

- **기능 명세서**: `src/ai/specs/recurring-events-spec.md`
- **관련 파일**:
  - `src/types.ts` (RepeatInfo에 id 필드 추가 필요)
  - `src/utils/recurrenceUtils.ts` (재사용, 수정 불필요)
  - `src/hooks/useEventOperations.ts` (3개 함수 추가 필요)
  - `src/App.tsx` (UI 활성화 및 다이얼로그 추가)
  - `server.js` (수정 불필요, API 이미 존재)

